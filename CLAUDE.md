# jobsite — 求人検索サイト jobs.agent-best.net

Airtableの求人を検索できる**静的な中途採用求人サイト「求人検索 — キャリアの選択肢」**（求職者向け・一般公開）。マイページ（会員機能）付き。
掲載は**中途のみ**（スナップショット456件のうち中途422件）。

- 公開URL: https://jobs.agent-best.net/ （GitHub Pages・HTTPS強制）
- リポジトリ: **Public**（機密なし＝求人データ／テンプレ／rebuild.js のみと確認済み）

## ⚠ ビルドフロー（最重要）

**`index.html` と `apply.html` を直接編集しない。** 編集するのは `template.html` / `apply-template.html` / `data/jobs.json`。

```
template.html / apply-template.html / data/jobs.json を編集
  → node rebuild.js      （index.html・apply.html・1day.html を再生成・件数を表示）
  → commit & push        （数十秒で反映）
```

| 生成物 | テンプレ | 差し込むもの |
|---|---|---|
| `index.html`（約3.3MB） | `template.html` | `__JOBS_DATA__` ＝ 中途求人422件の全項目 |
| `apply.html`（約135KB） | `apply-template.html` | `__JOBS_MINI__` ＝ ID・企業名・職種名・年収**だけ** |
| `1day.html` | `1day-template.html` | `__EVENTS_DATA__` |

⚠ **`apply.html` に求人データ全部を持たせない。** 申し込みフォームが必要なのは「どの求人から来たか」の
見出しだけなので、`rebuild.js` が最小限に削って埋めている。全部入れると3.5MBのフォームになる。

`index.html` は自己完結の静的HTML（CDN非依存）。詳細・Airtable取得元・フィールド対応は `README.md` にある。

## データ元

Airtable base「人材紹介事業」`appYkc36EvioYoL1A` / table「求人DB（求人票）」`tblyPZZasXTM2tcrV`（456件スナップショット。うち中途422件だけを載せる）。

## 1day選考会（`1day.html` ＋ 検索結果1位のPR枠）

⚠ **2026-09-01に導線を作り替えた。** 開催数が少ないのにナビ・特集行・フッター・専用ページと4か所を
占めていたため、**ナビ・一覧上部の特集行・フッターからは外し、検索結果の1位に固定で出るPR枠に一本化**した。
専用ページ `1day.html` は残っていて、PR枠のリンク先になっている（ナビからは辿れない）。

- PR枠は `template.html` の `prSlotHtml()`。**1ページ目のときだけ**先頭に差し込む。
- 開催があるときは日程・参加企業を出して `1day.html` へ、無いときは「開催の案内を受け取る」で
  `apply.html` へ送る（**常に表示する**）。
- PR枠に出すデータは `rebuild.js` の `onedayMini()` が `__ONEDAY_MINI__` に差し込む（直近3件）。
  **`data/1day.json` を更新したら `node rebuild.js` を回すこと。** 回さないとPR枠が古いままになる。


開催情報は Airtable「1day選考会」`tbl1J80CGqiOTvuf7`。`node fetch-1day.js` → `data/1day.json` →
`node rebuild.js` → `1day.html` の順。**求人と違い、取得は毎回 Airtable から手で回す。**

- 「公開ステータス」が**非公開・未設定**の回は `fetch-1day.js` の時点で落ちる（＝サイトに出ない）。
- ⚠ **このテーブルは shinsotsu.agent-best.net と共有している。**「区分」（2026-08-28追加・
  `fldfhFRnZhuug3rl0`）で振り分ける。中途＝このサイト、新卒・インターン＝shinsotsu。
  - `rebuild.js` の **`midCareerEvents()`** が中途以外を落とす。**shinsotsu 側には鏡写しの
    `newGradOnly1day()` がある。片方だけ直すと、同じ回が両サイトに出る／どちらにも出ない。**
  - **区分が空の回は中途扱い**（求人の `midCareerOnly()` と同じ既定）。ビルド時に回名を挙げて警告する。
  - 記入例の行（非公開）に運用メモを入れてある。**複製したら区分を必ず見直すこと。**
- 申し込みは Airtable フォーム `shr0K5l9MSiNMgUe6`（`applyUrl()` が回名と日付をプリフィル）。
  ⚠ **shinsotsu 側はこのフォームを使わず、自前の `apply.html` に送っている。**

## サイトの位置づけ・表記

- **中途採用（転職）のみを扱う**。2026-08-28に新卒・インターンの掲載をやめた（→「中途のみにする仕組み」）。
- **「ミドル・ハイクラス向けの中途採用（転職）求人検索サイト」**として訴求する。見出しは「その経験に、見合うポジションを。」
  - ⚠ 以前あった「**『転職サイト』とは書かない**（新卒・インターンを含むため）」というルールは、
    中途のみになったため2026-08-28に解除した。**新卒・インターンを再び載せるなら、この表記も戻すこと。**
  - 新卒・インターンを探している人の受け皿は、フッター「専門ページ」の
    [学生（新卒・インターン）向け](https://student.agent-best.net/)。**この相互リンクは消さない。**
- 年収800万以上/1,000万以上の件数はテンプレ内JSで**データ連動集計**（`salaryMax` 基準）。ハードコードしない。

### 中途のみにする仕組み

`rebuild.js` の **`midCareerOnly()`** が `kubun !== "中途"` の求人を落としてから埋め込む。
**Airtable 側に新卒・インターンが残っていても、サイトには絶対に出ない。**
除外した件数はビルド時にコンソールへ出る（例: `中途以外を除外しました: 34件（新卒 29件 / インターン 5件）`）。

- ⚠ **`data/jobs.json` は加工していない**（456件のまま）。消していないのは、Airtableを直したあと再エクスポートすれば
  自然に揃うのと、間違って落としたときに戻せなくなるのを避けるため。**「掲載を止める」判断はビルド側に置いてある。**
- 掲載が中途だけになったので、絞り込みの **「区分」ファセットは外した**（選択肢が1つしか出ないため）。
  求人カード・求人詳細の区分タグも `kubun !== "中途"` のときだけ出る＝通常は出ない。
- ⚠ **`midCareerOnly()` を外すと、新卒・インターンが「転職サイト」として公開される。**

### 第二新卒はこのサイトに載せる

**2026-08-28に方針決定。** 第二新卒は新卒ではなく**中途**なので、区分を「中途」にしてこのサイトに出す。
同日に4件の区分を「新卒」→「中途」に直し、掲載が 422件 → **426件** になった。

- ⚠ **区分を「新卒」にすると、どちらのサイトにも出ない**（こちらは `区分≠中途` で弾き、
  新卒サイト shinsotsu.agent-best.net は第二新卒を扱わないため）。**いちばん気づきにくい事故。**
- Airtable の `対象卒業年`（`fldPJIVJ0Tv4TQwaN`）に **「既卒・第二新卒」** が付いていれば第二新卒歓迎の求人。
  **このサイトはまだこの値を使っていない**が、「第二新卒歓迎」で絞りたくなったときに使える。

### ヒーロー（アイキャッチ）と OGP 画像

一覧ページ先頭の `.hero` は **写真＋藍のスクリム＋コピー＋検索窓＋人気の条件＋件数**。2026-08-28に追加。

| ファイル | 用途 |
|---|---|
| `assets/hero.jpg` / `assets/hero.webp` | ヒーロー背景（1600×650） |
| `assets/ogp.jpg` | SNSシェア用（1200×630）。`og:image` ＋ `twitter:card=summary_large_image` |

- 素材は **Pexels 16117376**（高層階の窓から見た東京）。**Pexels License ＝ 商用可・クレジット表記不要・改変可**。
  元は `https://images.pexels.com/photos/16117376/pexels-photo-16117376.jpeg?w=2400` を 2.46:1 に切り出したもの。
- ⚠ **人物が写っていない写真をあえて選んでいる。** 狙いは20代・30代の若手ハイクラス（アサインと同じ領域）で、
  人物写真を置くと年齢・性別の印象が固定されるため。**差し替えるときもここは踏襲する。**
- 2026-08-28の最初の版は PIXTA 139516443（コーポレートのトップ動画と同素材）だったが、**打ち合わせ中の人物写真は
  「管理職・経営幹部」寄りに見える**ため同日中に差し替えた。両方の出所は `C:\Users\user\hero-video\CREDITS.md`。
- OGP画像は HTML＋Chromeヘッドレスの `--screenshot`（1200×630）で作った。**`--screenshot` のパスは絶対パスで渡す。**
- ヒーローは**入り口専用で状態を持たない**。検索窓は `#q` に値を流し、人気の条件は `state` と左サイドバー
  （`#salRange` / `#salLbl` / `[data-remote]`）を更新してから `apply()` を呼ぶ。**ヒーロー側に独自の絞り込み状態を持たせない。**
- 中のコピーは常に白文字。**ライト・ダークで出し分けない**（写真の上なのでスクリムで担保している）。
  幅900px以下では左からのグラデーションをやめ、**全面を暗くするスクリムに切り替える**（コピーが写真の明るい側に重なるため）。
- ヒーローの文言は**20代・30代の若手ハイクラス向け**。小見出し「20代・30代のハイクラス転職」／
  説明文「転職はゴールではなく、キャリアの通過点。5年後の選択肢が増える一社かどうかで選べるように、求人を揃えています。」
  ヒーロー直下の注記も「掲載しているのは求人の一部です。重要なポジションほど公開されず、**非公開で動いています**。」に変えた。
  ⚠ **以前ここにあった「経営幹部・部長クラスを含む」は外した。** 若手ハイクラスを狙う見出しの直後にクラスの上下を出すと視線が戻るため、
  クラス名ではなく「なぜ非公開なのか」で書いている。
- `meta description` と `og:description` も若手寄りに直した（**title とロゴの MIDDLE & HIGH CLASS は据え置き**）。
  ⚠ 説明文に業界名を書くなら **`data/jobs.json` の実際の構成を数えてから**にする。多い順は
  IT・Web 170 ／ 住宅・インテリア 69 ／ 人材 52 ／ 総合・ITコンサル 38 で、**「コンサル中心」とは書けない**。
- ⚠ いまは**ヒーローと説明文だけ若手寄り、title とロゴはミドル・ハイクラスのまま**という中間状態。
  次に触るときは、どちらかに寄せるか判断する。
- デザイン: 藍×山吹、日本語ゴシック。**ワークポート／JACに寄せた求人サイト型**（左サイドバー常時表示の絞り込み＋1件1行の横長カード＋ページネーション＋別ページ相当の求人詳細）。ライト・ダーク対応。
- フッターに運営者情報: **株式会社エージェントベスト／東京都港区六本木4-8-7 嶋田ビル5階／有料職業紹介事業 13-ユ-316964**
- 応募CTAには「当社から企業へ推薦する形で選考が進む」「ご利用は無料（手数料は採用企業負担）」を明記する。

## 応募・相談の導線

テンプレ先頭の定数で管理: `APPLY_PAGE` / `INQUIRY_FORM_URL` / `CONSULT_URL` / `CONTACT_MAIL` / `PROFILE_URL`

**ワークポート／JACと同じ建て付けを採る**（2026-08-21に方針変更）。

```
求人カード／求人詳細の「応募する」
  → apply.html?job=<求人ID>（自社内の申し込みフォーム）
  → 転職支援サービスへの申し込み＝当社への登録
  → 当社から企業へ推薦する形で選考が進む
```

- **主CTA**「応募する」／「この求人に応募する」→ `apply.html`（`applyHref(j)`）
- **従CTA**「まず話だけ聞いてみる」→ Calendly（`consultUrl()`）
- 「電話で軽く話を聞きたい」「メールで質問する」→ 問い合わせフォーム（`inquiryUrl(j, WAY_TEL/WAY_MAIL)`）
- 「企業サイト」は目立たせない方針（企業情報dlの1行リンク）

### ⚠ 2026-08-16の「応募と書かない」ルールは撤回した

以前は「押した先で起きるのは応募ではなく推薦依頼だから『応募する』と書かない」というルールを置いて、
応募導線そのものを消していた。**2026-08-21に、すでに成功しているワークポート／JACの型に寄せる方針で撤回。**

撤回できる理由は、**実態とのズレをボタンの文言ではなくフォーム側で埋めているから**:

- 申し込みフォームの見出しが「**転職支援サービスに申し込む**」であって「応募フォーム」ではない
- 求人詳細・右レール・フォーム内の3か所に「企業へ直接応募するのではなく、**当社から推薦する形で選考が進む**」と明記
- 利用規約とプライバシーポリシー（**人材紹介**の建て付け）への同意を、送信前に**個別のチェックボックス**で取る

**この3つのどれかを削るなら、「応募する」の表記も一緒に見直すこと。** 文言だけ残すと、
求人広告モデル（応募行為＝同意）を想起させたまま人材紹介の実態で動くことになる。

### 申し込みフォーム `apply.html`

4ステップ。①お名前・生年月日 ②直近の経験職種・都道府県・現年収 ③メール・携帯 ④面談方法・転職時期・状況・同意。

⚠ **面談方法は「オンライン」「電話」の2つだけ。対面は載せない**（実際に受けていないため）。ワークポートは全国50拠点の対面を選ばせるが、そこは真似しない。増やすなら実際に受けられる形だけにする。

- **面談日時は聞かない。** ワークポートは拠点50か所の空き枠から選ばせるが、当社は拠点1つで枠管理をしていない。
  送信後の完了画面で Calendly（`calendly.com/r_matsuoka`）を案内し、選びたい人だけ選ぶ形にしている
- 送信先は **Supabase `public.applications`**（匿名キーで INSERT のみ）。設定は `APPLY_SETUP.md`
- ⚠ **SELECTポリシーを作らない。** 匿名キーは `apply.html` に書いてあるので、SELECTを1つでも足すと
  応募者の氏名・生年月日・電話番号が全世界から読める
- 入力途中の内容は `localStorage`（`jobsite-apply-draft`）に保存。送信成功で消す
- マイページにログイン中なら、氏名・メール・転職時期を先に埋め、`user_id` も一緒に送る
- ハニーポット（画面外の `website` 欄）が埋まっていたら送信しない
- 通知は Supabase Webhook → Apps Script → Gmail（`C:\Users\user\jobsite-notify\`。**Gitに入れない**）

### 求人詳細は「別ページ」として扱う

スライドパネルをやめ、`?job=<求人ID>` を **pushState** で積む別ビューにした（`openDetail` / `goDetail` / `goList` / `route`）。
戻るボタン・URL共有・履歴が普通のページと同じように動く。⚠ **`replaceState` に戻すと「戻る」で一覧に帰れなくなる。**

### 一覧はページネーション（無限スクロールにしない）

1ページ 30／50／100件（`perPage`）。条件を変えながら探す人が「何ページ目を見ていたか」で戻れるようにするため。

### ⚠ 「新着順」と NEW バッジは、データに `createdAt` があるときだけ出る

`data/jobs.json` に `createdAt`（AirtableのcreatedTime相当）が入っていれば、
並び替えに「新着順」が増えて既定になり、掲載14日以内（`NEW_DAYS`）の求人に NEW バッジが付く。
**いまの jobs.json には入っていないので、どちらも出ない**（`HAS_DATES` が false）。
件数や新しさを偽らないための仕様。出したければ jobs.json を作り直すときに `createdAt` を足す。

**電話は Calendly ではなくフォーム**。Calendlyの電話面談15分は「日時を選ぶ」必要があり、「今すぐ軽く聞きたい」人には重い。フォームは**日程を決めず、希望時間帯だけ聞いて折り返す**。

問い合わせの受け皿は `求人の問い合わせ（サイト）` `tbltITsj4OmX5B2PJ`。**応募（apply.html）とは別物で、履歴書・職務経歴書は受け取らない（意図的）**。⚠ `INQUIRY_FORM_URL` が空のあいだは `mailto:` にフォールバックする。フォームの作り方と、プリフィルが壊れる条件は `README.md`「問い合わせフォーム」にある。

問い合わせが入ると Airtable のオートメーション `wflaOu5LOuWTDwia1` が Slack **#notification_siteoubo** へ通知する。⚠ **APIから作ったオートメーションは下書きのままなので、Airtableの画面でオンにしないと動かない。**

⚠ Airtable の「求人応募（サイト）」`tblmHnF3Vq1WXT9ix` には**もう何も流れない**。応募の受け皿は Supabase に移った。
CRM側に集約したくなったら、Apps Script の中から Airtable API を叩くのが一番手数が少ない（`APPLY_SETUP.md` の第5項）。

`consultUrl(j)` は求人名・企業名を Calendly の事前質問1問目に `a1=` で渡す。⚠ **松岡アカウントの面談メニュー（WEB面談 `meet-with-me` / 電話面談 `call`）の1問目を差し替えると、この受け渡しが意味を失う。**

⚠ 問い合わせフォームは求人名・企業名・求人レコードIDを `prefill_◯◯` で自動入力しているため、**Airtable側のフィールド名を変えるとプリフィルが壊れる**。

## プライバシーポリシーは2本立て（混ぜない）

| | ファイル | 立場 |
|---|---|---|
| 人材紹介（有料職業紹介） | `privacy.html` | 当社が**ご本人を求人企業へ推薦**する。同意した企業にだけ情報提供 |
| 求人広告掲載（募集情報等提供） | `privacy-ad.html` | 当社は**掲載と取り次ぎのみ**。選考の当事者ではない。応募＝当該企業への提供に同意 |

**最大の違いは「第三者提供の建て付け」**。紹介＝個別同意、広告＝応募行為＝同意。ここを混ぜない。
両方に共通で職業安定法の記載（収集しない情報＝人種・民族・社会的身分・門地・本籍・出生地、思想信条、労働組合加入状況）がある。
**法務チェックは未実施。**
解析ツールを追加・変更したら**プライバシーポリシー第9項（GA利用）も必ず直す**。

## マイページ（会員機能）— 2026-08-15公開

### 設計の要（変えるときは理由を確認する）

**2段階で動く1つのコード**。`template.html` 冒頭の定数が空か否かで挙動が切り替わる。

```javascript
const SUPABASE_URL = "";        // 空 = この端末に保存モード（localStorage）
const SUPABASE_ANON_KEY = "";   // 埋める = メールログインモード（今ここ）
```

鍵を空に戻せばログイン無しモードに即戻せる。**障害時の退避路として覚えておく。**

機能: 気になる求人★／最近見た求人／会員プロフィール／履歴書・職務経歴書アップロード／退会。

★は「お気に入り」ではなく**「気になる」**と呼ぶ。保存の語ではなく状態の語で、まだ転職を決めていない層に押す心理的コストが低い。⚠ **表示名だけの話で、内部の識別子（localStorageキー `jobsite-favs` / Supabaseテーブル `favorites` / GA4イベント `fav_add`・`fav_remove`）は変えていない。変えると既存の保存が消え、計測の連続性も切れる。**

### 潜在層の獲得（2026-08-16）— 応募数ではなく「シグナル×連絡可能」を取る

求人サイトのKPIを応募数に置くと、まだ転職を決めていない層は取れない（応募しないため）。★を**興味のシグナル**として受け取り、こちらから連絡できる状態を作るのが狙い。

- ★が `SIGNAL_AT`（=2）件たまると、その場に登録案内パネル `#signal` を出す。**1件目では出さない。** ✕でも「今はしない」でも辞退＝`jobsite-signal-off` を立てて**二度と出さない**
- ⚠ **未ログインの★は `localStorage` にしか無く、こちらからは見えない。** 連絡可能にするには身元が要る、というのがこの設計の前提

### ⚠ 求人案内メールの同意（緩めない）

- **受信同意は利用規約への包括同意と分けて、個別に取る**（特定電子メール法のオプトイン）。チェックボックスは**既定でオフ**、チェックしなくても会員登録は完了する
- 保存先は `profiles.contact_consent` / `contact_consent_at`。⚠ **`contact_consent` が `true` の人にだけ送る**
- 同意は確認リンク送信時に auth のユーザーメタへ預け、**本人がリンクを開いてログインが成立した時点で** `profiles` に書き移す（`onSignedIn`）。リンクを開かなかった人の同意はどこにも残らない
- 配信停止はマイページの「求人のご案内」のチェックを外して保存。**停止の導線は必ず残す**
- ⚠ `SUPABASE_SETUP.md` の受信同意SQLが未実行だと列が無い。その場合 `hasConsentCols` が `false` になり、同意欄を隠して**プロフィール本体だけは壊さず動く**ようにしてある。**この退避を外すと、SQL未実行の環境で会員のプロフィールが空に見え、保存で上書きされる。**

### Slack通知（2026-08-16）

会員が★を押すと Slack に通知が飛ぶ。`favorites` の INSERT → Database Webhook → Edge Function `supabase/functions/notify-slack/index.ts` → Slack Incoming Webhook。手順は `SUPABASE_SETUP.md`。

- ⚠ **通知できるのはログイン済みの★だけ。** 未ログインの★は `localStorage` にしか無く、`privacy.html` 第2項・第11項で**「当社が取得することはありません」「当社のサーバーへは送信されません」**と明記している。**記載を変えない限り匿名の★は通知できない**（変えるなら、それは信頼設計の後退だと理解したうえで判断する）
- ⚠ **Slack の Webhook URL は Edge Function の Secret に置く。サイトには絶対に貼らない**（Publicリポジトリ）
- ⚠ **`HOOK_SECRET` を省略しない。** 関数のURLは誰でも叩けるので、無いと偽の通知を流し込まれる
- 通知には受信同意の有無が出る。**`🚫 受信同意なし` の人に求人案内を送らない**

### ⚠ セキュリティ上、絶対に緩めてはいけないもの

- **Storage の RLS 4本**（select/insert/update/delete）。条件は
  `bucket_id='documents' and (storage.foldername(name))[1] = auth.uid()::text`。
  これとバケット非公開が「他人の履歴書を見られない」の担保。**消すと全員の書類が漏れる。**
- **`delete_own_account()` の where 句**。`security definer` 関数で `auth.uid()` の行しか消せないようにしてある。**緩めると他人のアカウントが消せる。**
- **Secret key / service_role は絶対にサイトに貼らない。**

### 実装メモ（ハマりどころ）

- 求人カードは `.jrow`（`<article>`）。**カード全体はクリックできない**（タイトルと「詳細を見る」がリンク、「応募する」が別リンク）。ワークポート／JACと同じで、押した先が2つあるカードを丸ごとクリック可能にすると誤爆する。
- ★は歴史的経緯で `<span role="button" tabindex="0">`（旧カードが `<button>` だった名残）。`#grid` の委譲ハンドラで `.fav` を先に判定して `return` している。
- ★のクリック判定は `#grid` 用と document 用の**2系統**（マイページ内・求人詳細の右レールの★は `#grid` の外にあるため）。
- スマホでは★のタップ範囲を42pxに拡大済み（32pxだと押し外す）。
- 希望職種・経験職種は求人データから自動生成。`j.jobGroup` ＝ `jobCategory` 末尾の括弧（「法人営業（営業）」→「営業」）で22分類。⚠ **`jobCategory` の命名規則（末尾の括弧＝大分類）を崩すと壊れる。**
- 希望勤務地は都道府県ではなく**9つの地域ブロック**（`AREA_BLOCKS`）。求人があるのは11都府県だけなので都道府県で聞くと選択肢が偏る。
- 書類の閲覧は `createSignedUrl(path, 60)`。**`window.open` は await の前に同期で開く**（ポップアップブロック回避）。
- 削除の順番は **①書類の実体（Storage API）→②DBの行→③auth.users**。逆にするとファイルが残る。
- **`confirm()` は使わない**（ブラウザ操作が固まるため）。ページ内のボタンで二段階にする。
- `saveProfile` は `myProfile` を**マージ更新**する（上書きすると画面から書類が消える）。
- 最近見た求人は `jobsite-seen`（localStorage）に直近15件。**ログインしてもサーバーへは同期しない。**

### Supabase

| 項目 | 値 |
|---|---|
| プロジェクト | `jobsite-tokyo` / ID `jvdnabtpxcyfnogdulea` |
| リージョン | **Northeast Asia (Tokyo)** |
| 鍵 | **Publishable key**（`sb_publishable_…`）。旧anonキーではない |

⚠ **リージョンの罠**: Regionで「Asia-Pacific」を選ぶと**Singaporeになる**。「SPECIFIC REGIONS」から Northeast Asia (Tokyo) を明示的に選ぶこと。後から変更不可。`privacy.html` に「日本国内のデータセンターに保管」と書いてあるので、東京でないと記載と矛盾する。

## 企業ロゴ（求人カード・求人詳細）

流れは **Airtable「求人DB（企業）」のロゴ列 → `node fetch-logos.js` → `assets/logos/` ⬏ `data/logos.json`
→ `node rebuild.js` の `attachLogos()` が求人1件ずつに `logo` を差し込む → `logoHtml(j)`**。
画像が無ければ社名の頭文字タイル（`coInitial`）になる。2026-08-31に公開。

- ⚠ **ロゴを `data/jobs.json` に書かない。** jobs.json は Airtable から取り直すたび丸ごと入れ替わるスナップショットなので、書くと毎回消える。対応表は `data/logos.json`（企業名 → パス）に分けてある
- ⚠ **外部の favicon サービス（`google.com/s2/favicons` 等）は使わない。** 訪問者がどの求人を見たかが第三者に渡る。シェアボタンで外部SDKを避けているのと同じ理由
- ⚠ **Airtable の添付URLは数時間で失効する。** 静的サイトから直接参照できないので、必ず画像をリポジトリに取り込む
- 企業名の**完全一致**で引いている。Airtable で社名を直したら `logos.json` も直す（黙って頭文字タイルに戻る）
- ⚠ **新卒サイト（shinsotsu）に同じ仕組みが入っている。** `attachLogos()`・`logoHtml()`・`assets/logos/`・`data/logos.json` が鏡写し。片方だけ直すと、同じ企業のロゴが片方のサイトにだけ出る
- 2026-08-31時点で **掲載中の求人が参照する24社すべてにロゴがある**（中途426件＝全件・新卒24件＝全件に表示）。頭文字タイルは、いまは新しい企業を足したときの受け皿として動いている
- ⚠ **`lts.png` と `bridgeone.png` は手でトリミングしてある。** Airtable に入っているのが LTS は
  **OGPバナー（1200×630・余白とタグライン「可能性を解き放つ」入り）**、ブリッジワンは**余白の多い250×250**で、
  そのままタイルに入れるとロゴが極小になるため。ロゴ部分だけを切り出した（PowerShell の System.Drawing で実施）。
  **`node fetch-logos.js` を回すと Airtable の原本で上書きされて余白が戻る。** 回したあとはこの2社の見え方を必ず確認する
  （根本的に直すなら、Airtable 側の添付をトリミング済みの画像に差し替えてもらう）
- `coInitial` は「株式会社」等を除去してから先頭を取る。英数字は2文字、日本語は1文字
- ⚠ **タイルの幅は画像のときだけ可変（`--img`）。** ロゴは横長のワードマークが多く、28px角に押し込むと高さ5pxほどになって読めない。頭文字タイルは正方形のまま
- ⚠ **画像の高さは % ではなく px で指定する。** `display:grid` のタイルの中で `height:100%` にすると親の高さが内容依存で循環し、画像がタイルからはみ出す（実際に踏んだ）

## 従業員数・企業規模の絞り込み（2026-09-05）

流れは **Airtable「求人DB（企業）」の 従業員数 ＋ 従業員数（数値） → `node fetch-employees.js` → `data/employees.json`
→ `node rebuild.js` の `attachEmployees()` が求人1件ずつに `employees`（原文）と `employeeCount`（人数）を差し込む
→ `template.html` の `EMP_BANDS` が人数を段に振り分ける**。ロゴと同じ建て付け。

Airtable 側に列が2つある。

| 列 | 型 | 役割 |
|---|---|---|
| `従業員数`（`flda7sYQBsb05X781`） | テキスト | **原文**。「連結3,039名、単体835名（2026年4月1日現在）」など自由記述 |
| `従業員数（数値）`（`fldTazycQVisRgCpR`） | 数値 | **人数。絞り込みはこれが正**（2026-09-05に491社ぶん整備） |
| `企業規模`（`fldPd6KbT9SAlC40V`） | 単一選択 | Airtable上で規模別に見るための段。サイトは使わず、人数から同じ境界で計算する |

| 出す場所 | 出すもの |
|---|---|
| 左サイドバーの絞り込み「企業規模（従業員数）」 | `EMP_BANDS` の段（〜50名 / 51〜100名 / 101〜300名 / 301〜1,000名 / 1,001名以上） |
| 求人カードの `jrow__meta` | **段**（例「101〜300名」） |
| 求人詳細の「企業情報」 | **Airtableの原文そのまま**（例「約290名（2025年9月）」） |

- ⚠ **求人詳細には原文を出す。** 「連結3,039名、単体835名」「約37名（グループ含むと約180名）」のように
  但し書き付きで書かれているので、1つの数字に丸めると**当社が勝手に丸めた人数を企業の公表値のように見せる**ことになる。
  数値列は絞り込みとカードの段にだけ使う
- ⚠ **サイト側で原文から人数を推測しない**（2026-09-05に推測パースをやめて Airtable の数値列に一本化した）。
  人数が空の企業は段に入らず、企業規模で絞ると出ない。それが正しい状態で、**直すのは Airtable の「従業員数（数値）」列**。
  推測で段に入れると、絞り込みの結果が事実と食い違う
- ⚠ **`data/jobs.json` に書かない。** ロゴと同じ理由で、jobs.json は取り直すたび丸ごと入れ替わる
- ⚠ **段の境界は `template.html` の `EMP_BANDS` と Airtable の「企業規模」列の2か所にある。変えるなら両方合わせる。**
  サイトが Airtable の段の列を読まないのは、切り方を変えるたびに491社を付け直さずに済ませるため
- `fetch-employees.js` は**原文から読んだ人数と数値列が食い違う企業を名指しで警告する**。
  原文だけ直して数値列を直し忘れると、古い人数で絞り込ませたままになるため（値としては使わない・照合専用）
- 絞り込みの並びは件数順ではなく**人数の小さい順**。`facetDefs` に `order` を持たせた facet だけこの並びになる
- Airtable の一括投入スクリプトは `端末0\【求人DB】AirTable｜DB加工用\従業員数_書き直し\`（`fill.js`・元の値のバックアップ付き）
- ⚠ **新卒サイト（shinsotsu） に同じ仕組みが入っている。** `fetch-employees.js`・`attachEmployees()`・`EMP_BANDS`・
  `data/employees.json` が鏡写し。片方だけ直すと、同じ企業が片方のサイトでだけ段に入る
- 2026-09-05時点で**中途426件すべてが段に入る**（〜50名 52 / 51〜100名 18 / 101〜300名 187 / 301〜1,000名 93 / 1,001名以上 76）。Airtable 全体では777社中496社に人数が入っている。**増やすには Airtable の「従業員数（数値）」列を埋める**（原文だけ埋めても段には入らない）

## シェアボタン

X / Facebook / LinkedIn / はてなブックマーク / リンクをコピー（＋スマホは端末の共有シート）。`template.html` の `shareHtml()`。

- **外部SDK（Twitter widgets.js・Facebook SDK）は読み込まない。** 読者の閲覧履歴が各社に渡るうえ表示も遅い。ただのリンクとして自前で持つ。
- **コピーは合成クリック（`.click()`）では必ず失敗する。** テストは実クリックで確認すること。
- 求人ごとのURLは `?job=<求人ID>`。求人詳細を別ページ扱いにしたので **pushState**（戻るで一覧に帰れる）。**OGPは求人ごとに出し分けできない**（静的サイト＝1つのHTML）ので、求人名はX投稿の本文側で補っている。`document.title` は求人ごとに書き換えている。

## GA4

測定ID `G-1XXMP8Y1B4`。カスタムイベント: `job_detail_open` / `apply_click` / `apply_form_open` / `apply_step` / `apply_submit` / `apply_error` / `inquiry_click` / `consult_click` / `search` / `filter_use` / `page_change` / `perpage_change` / `lp_click` / `corporate_click` / `job_share` / `fav_add` / `fav_remove` / `signal_shown` / `signal_submit` / `signal_dismiss`。

**応募のファネルは `apply_click`（一覧・詳細で押した）→ `apply_form_open`（フォームに着いた）→ `apply_step`（step2〜4に進んだ）→ `apply_submit`（送信できた）** で見る。`apply_step` の落ち方で、どの設問が重いか分かる。

⚠ **`apply_click` は `a[data-apply]` の capture ハンドラで取る。** `apply.html` はサイト内リンクなので、外部リンク用の一括計測には乗らない。**`data-apply` 属性を消すと応募のクリックが計測できなくなる。**

⚠ **`inquiry_click` は `way` で電話（`tel`）とメール（`mail`）を分けている。** 「電話で軽く話を聞きたい人がどれだけいるか」を測るための軸なので、消さないこと。`airtable.com` へ出ていくのは問い合わせフォームだけになったので、ホスト名判定も `inquiry_click` に寄せてある。
外部リンクのクリックは document の capture で一括計測しているので、ボタンを追加しても漏れない。`track()` は gtag 未読込でも落ちない。

## 相互リンク

トップのピル型「専門ページ」とフッターから求職者向けLPへ。各LP側もナビ＋フッターに「求人を探す↗」がある。
求人詳細の最下部にある「この求人を担当するエージェント」ブロックは、`agentbest/agentbest-lp` の `AuthorBlock.astro` からのコピー＝**同じ文章が複数リポジトリに重複**している。略歴・許可番号を直すときは全部直すこと。

## push のルール

- ローカルで **`node rebuild.js` を通してブラウザ確認してから** commit & push。コミットメッセージは日本語。**push後は必ず何を変えたか報告する。**
- ローカル確認は `python -m http.server` などHTTPで開く（`file://` だと `history.pushState` が効かず、求人詳細の遷移が確認できない）。
- **以下に触れるときは必ず止まって事前確認する**:
  1. ドメイン・DNS・CNAME
  2. **個人情報・フォーム・認証**（マイページのログイン、RLS、応募フォームは全部ここ）
  3. 費用が発生する変更（Supabase Pro への切替など）
  4. 既存ページ・データの削除、求人データの一括置換
  5. 複数リポジトリへの一括変更
- Publicリポジトリ。push前にトークン・APIキーの混入をgrepで確認する（**Supabase の Secret key が入っていないこと**を特に確認）。
